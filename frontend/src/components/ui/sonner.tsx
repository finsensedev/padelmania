/**
 * Legacy Sonner wrapper retained for backwards compatibility.
 * Notifications are now powered by Notistack via NotificationProvider.
 * This module intentionally exports nothing to avoid runtime usage.
 */

export {};
